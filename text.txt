

cd Front-and
cls
npm install
npx expo start --port 8085

cd Back-end; cls; py -m main

cd Back-end; cls; py -m main-generator


cls; py -m worker_client

py -m run_backend  

back end

"dmbr caha icgq zbsh"

# Spusti všetko v pozadí
docker-compose up -d

# Zobraz logy backendu
docker-compose logs -f backend

# Zobraz logy databázy
docker-compose logs -f postgres

# Zastav všetko
docker-compose down

# Vymaž všetko (aj dáta)
docker-compose down -v


# Vytvorte virtuálne prostredie
python3 -m venv venv

# Aktivujte ho
source venv/bin/activate

# Nainštalujte závislosťami
pip install -r requirements.txt